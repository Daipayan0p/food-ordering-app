import 'package:flutter_test/flutter_test.dart';
import 'package:food_delivery_app/src/app/api/customer_api.dart';

void main() {
  group('CustomerApi Tests', () {
    test('getFood should return a list of FoodItems', () async {
      // Act
      // final res = await CustomerApi.getFood();

      // Assert
      // expect(res, isNotEmpty); // Check if the list is not empty
      // print(res[0].id); // Print the ID of the first food item
    });
  });
}
